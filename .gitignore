# Learner-private state — never leaves the machine
progress/*.local.md
gym-app/.session.json
gym-app/.chats/

# The practice sandbox is provisioned from sandbox-seed/ as the learner's own
# git repo; their work-in-progress stays local (see `task setup-sandbox`).
sandbox/

# Stray local captures
screenshots/

# Dependencies / build
node_modules/
gym-app/**/node_modules/
gym-app/web/dist/
*.log

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
