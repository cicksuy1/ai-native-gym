# Sandbox working agreement

A couple of standing rules for working in this sandbox. Later gym modules (Review, Ship-a-Feature)
have you *add* to this file when a review catches a mistake worth preventing — that's the point.

- Keep the suite green: run `python -m unittest` before you call a change done.
- No silent failures — user-facing commands print a clear message, never swallow an error.
